[project]
name = "assistant-workspace"
version = "0.1.0"
description = "Personal Second Brain - AI Assistant Workspace"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.0",
]