# Claude Second Brain v4.0 - 完整实现方案

## 一、需求分析总结

### 核心目标
构建 **以 Claude Code CLI 为中心、以 Python 脚本为执行工具的自动化项目管理与知识沉淀系统**。

### 关键要求
1. ✅ **非侵入式捕获**：监控对话流水，保存完整底稿
2. ✅ **语义化分发**：AI 归纳后精准分类到 KB/Todo/Plan
3. ✅ **全闭环交互**：AI 既是记录者也是执行者
4. ⚠️ **历史追溯**：读取完整 history.jsonl（存在权限限制）
5. ⚠️ **讨论模式**：支持提案式交互，避免误写入

---

## 二、现状评估

### 已实现
| 模块 | 功能 | 完成度 |
|-----|------|--------|
| Hook 系统 | SessionStart/Stop 事件监听 | 70% |
| 对话记录 | 保存到 raw/ | ✅ 100% |
| 记忆总结 | 生成 memory/ 日志 | 60% |
| 分发执行 | 写入 KB/Tasks/Plan | 40% |
| 日志系统 | loguru 记录 | ✅ 100% |

### 核心问题
1. **Hook 配置错误**：
   - 当前仅监听 `Stop` 事件
   - 需要同时监听 `UserPromptSubmit` 获取用户输入
   - 需要 `SessionStart:clear` 清除上次缓存

2. **语义理解缺失**：
   - 当前仅做文本截断 (`[:280]...`)
   - 未利用 AI 能力进行内容分类和提取
   - 知识库充斥无用片段

3. **历史追溯受限**：
   - Claude CLI 的 `~/.claude/history.jsonl` 可能无权限读取
   - 需要设计降级方案

4. **讨论模式未实现**：
   - CLAUDE.md 提到但无代码支持
   - 需要检测【讨论】标记并分支处理

---

## 三、系统架构设计

### 五层架构

```
┌─────────────────────────────────────────────┐
│  Layer 1: 捕获层 (Capture Layer)            │
│  - SessionStart: inject_context.py          │
│  - UserPromptSubmit: capture_user.py        │
│  - Stop: save_conversation.py               │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│  Layer 2: 存储层 (Storage Layer)            │
│  - raw/: 原始对话流水 (只增不改)            │
│  - memory/: 每日结构化总结                  │
│  - logs/: 系统日志 (loguru)                │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│  Layer 3: 分析层 (Analysis Layer) - 新增    │
│  - semantic_analyzer.py: AI 语义分类器      │
│    ├─ 检测讨论模式                          │
│    ├─ 提取核心知识点                        │
│    ├─ 识别任务/截止日期                     │
│    └─ 生成行动计划                          │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│  Layer 4: 分发层 (Distribution Layer)       │
│  - dispatcher.py: 精准路由器                │
│    ├─ knowledge_base.md (知识条目)          │
│    ├─ tasks.json (任务清单)                 │
│    └─ daily_plan.md (行动计划)              │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│  Layer 5: 整理层 (Curation Layer) - 新增    │
│  - daily_digest.py: 每日深度复盘            │
│  - weekly_review.py: 每周总结与规划         │
└─────────────────────────────────────────────┘
```

---

## 四、模块详细设计

### 模块 1: 历史追溯器 (History Reader)

**文件**: `hooks/inject_context.py`（已存在，需增强）

**职责**:
1. 读取最近 N 天的 memory/ 记忆
2. ~~读取 ~/.claude/history.jsonl~~（权限受限，放弃）
3. 生成上下文摘要注入当前会话

**增强点**:
```python
def get_recent_memory(days: int = 3) -> str:
    """优化：增加天数，增加智能摘要"""
    # 1. 读取 memory/
    # 2. 提取关键决策和未完成任务
    # 3. 生成简洁上下文（控制在 2000 字符以内）
```

---

### 模块 2: 指令中枢 (Instruction Injector)

**文件**: 
- `.claude/CLAUDE.md`（已存在，需完善）
- `.claude/settings.json`（已存在，需修正）

**settings.json 完整配置**:
```json
{
  "hooks": {
    "SessionStart:clear": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/inject_context.py"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/capture_user.py"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/save_conversation.py"
          }
        ]
      }
    ]
  }
}
```

**CLAUDE.md 增强**:
- 添加"讨论模式"指令说明
- 添加触发词规则（如"记录这个"、"总结一下"）

---

### 模块 3: 分发执行器 (Dispatcher) - 重构

**文件**: `hooks/save_conversation.py`（需重构）

**新流程**:
```python
def main():
    # 1. 读取用户输入 + 助手回复
    user_msg, assistant_msg = get_messages()
    
    # 2. 保存原始记录（不变）
    save_to_raw(user_msg, assistant_msg)
    
    # 3. 检测讨论模式
    if is_discussion_mode(user_msg):
        save_to_proposals(user_msg, assistant_msg)
        logger.info("讨论模式：内容已保存到 proposals/，不自动分发")
        return
    
    # 4. 调用语义分析器（新增）
    analysis = semantic_analyze(user_msg, assistant_msg)
    
    # 5. 精准分发
    dispatch_to_knowledge(analysis.knowledge_points)
    dispatch_to_tasks(analysis.tasks)
    dispatch_to_plan(analysis.action_items)
    
    # 6. 生成每日总结
    append_to_memory(analysis.summary)
```

---

### 模块 4: 语义分析器 (Semantic Analyzer) - **新增核心**

**文件**: `hooks/semantic_analyzer.py`（新建）

**职责**: 利用本地 Claude API 或规则引擎进行内容理解

**方案 A: 基于规则引擎**（快速，无需 API）
```python
def semantic_analyze(user_msg: str, assistant_msg: str) -> Analysis:
    """基于关键词和句法分析"""
    analysis = Analysis()
    
    # 知识提取
    if any(kw in full_text for kw in ["原理", "是什么", "解释", "概念"]):
        analysis.knowledge_points = extract_definitions(full_text)
    
    # 任务提取
    if any(kw in full_text for kw in ["要做", "记得", "任务", "TODO"]):
        analysis.tasks = extract_tasks_with_nlp(full_text)
    
    # 计划提取
    if any(kw in full_text for kw in ["下一步", "计划", "接下来"]):
        analysis.action_items = extract_action_items(full_text)
    
    return analysis
```

**方案 B: 调用 Claude API**（精准，需要 API key）
```python
def semantic_analyze_with_ai(user_msg: str, assistant_msg: str) -> Analysis:
    """调用本地或 Anthropic API"""
    prompt = f"""
    分析以下对话，提取：
    1. 核心知识点（技术概念、原理、最佳实践）
    2. 明确的任务（带截止日期）
    3. 行动计划（下一步要做的事）
    
    对话内容：
    用户：{user_msg}
    助手：{assistant_msg}
    
    请以 JSON 格式输出：
    {{
      "knowledge": ["知识点1", "知识点2"],
      "tasks": [
        {{"title": "任务名", "due": "2026-04-05", "priority": "high"}}
      ],
      "actions": ["行动1", "行动2"]
    }}
    """
    
    response = call_claude_api(prompt)
    return parse_json(response)
```

**推荐**: 先用方案 A（规则引擎），后期升级到方案 B

---

### 模块 5: 周期性归纳器 (Daily Summarizer) - **新增**

**文件**: `scripts/daily_digest.py`（新建）

**职责**: 每日结束时深度复盘

**触发方式**:
1. 用户手动：`python .claude/scripts/daily_digest.py`
2. 定时任务：cron 每天 23:00 执行

**核心逻辑**:
```python
def generate_daily_digest():
    today = get_today()
    
    # 1. 读取今日所有 raw 记录
    raw_content = read_file(f"raw/{today}_raw.md")
    
    # 2. 调用 AI 深度总结
    digest = call_claude_for_digest(raw_content)
    
    # 3. 生成结构化报告
    report = f"""
    # {today} 日终总结
    
    ## 今日成果
    {digest.achievements}
    
    ## 遗留问题
    {digest.blockers}
    
    ## 明日计划
    {digest.tomorrow_plan}
    
    ## 知识收获
    {digest.learnings}
    """
    
    # 4. 覆盖写入 memory/{today}.md
    overwrite_file(f"memory/{today}.md", report)
    
    # 5. 自动 promote 到知识库
    auto_promote_digest(digest)
```

---

## 五、实施计划

### Phase 1: 修复现有问题（1 天）
- [ ] 修正 `settings.json` Hook 配置
- [ ] 新增 `capture_user.py` 捕获用户输入
- [ ] 修复 `save_conversation.py` 的消息读取逻辑
- [ ] 测试 Hook 事件流是否正常

### Phase 2: 实现语义分析（2 天）
- [ ] 创建 `semantic_analyzer.py`（基于规则引擎）
- [ ] 重构 `save_conversation.py` 调用分析器
- [ ] 优化 knowledge_base/tasks/plan 的写入格式
- [ ] 测试内容分类准确性

### Phase 3: 讨论模式支持（1 天）
- [ ] 在 `save_conversation.py` 中检测【讨论】标记
- [ ] 创建 `proposals/` 目录存储讨论内容
- [ ] 更新 CLAUDE.md 说明讨论模式用法

### Phase 4: 日终总结（2 天）
- [ ] 创建 `daily_digest.py` 脚本
- [ ] 集成 Claude API 或使用规则模板
- [ ] 设置 cron 任务（可选）
- [ ] 测试日终报告质量

### Phase 5: 历史追溯增强（1 天）
- [ ] 尝试读取 `~/.claude/history.jsonl`
- [ ] 如失败，设计基于项目内 memory 的降级方案
- [ ] 优化 `inject_context.py` 的上下文生成

---

## 六、关键技术决策

### 1. 是否调用 Claude API？
| 方案 | 优点 | 缺点 | 推荐度 |
|-----|------|------|--------|
| 纯规则引擎 | 快速、免费、离线 | 准确性低 | ⭐⭐⭐ 初期 |
| Claude API | 精准、灵活 | 需要 API key、成本 | ⭐⭐⭐⭐⭐ 后期 |
| 混合方案 | 平衡 | 复杂度高 | ⭐⭐⭐⭐ 最优 |

**建议**: 初期用规则引擎，待验证价值后接入 API

### 2. history.jsonl 读取策略
- **方案 A**: 直接读取 `~/.claude/history.jsonl`（可能无权限）
- **方案 B**: 仅依赖项目内 `memory/`（安全但信息有限）
- **推荐**: 尝试 A，失败则降级到 B

### 3. 讨论模式实现
- 检测规则：消息开头包含 `【讨论】` 或 `【先讨论】`
- 处理方式：保存到 `proposals/` 而非自动分发
- 后续：用户确认后手动调用 `promote_proposal.py` 提升到正式库

---

## 七、文件结构

```
AssistantWorkspace/
├── .claude/
│   ├── CLAUDE.md               # 指令文档
│   ├── settings.json           # Hook 配置
│   ├── hooks/
│   │   ├── inject_context.py       # [模块1] 历史追溯
│   │   ├── capture_user.py         # [新增] 捕获用户输入
│   │   ├── save_conversation.py    # [重构] 保存+分发
│   │   └── semantic_analyzer.py    # [新增] 语义分析
│   ├── scripts/
│   │   ├── daily_digest.py         # [模块4] 日终总结
│   │   ├── weekly_review.py        # [可选] 周总结
│   │   └── promote_proposal.py     # [新增] 提案提升
│   ├── raw/                    # 原始对话流水
│   ├── memory/                 # 每日结构化总结
│   ├── proposals/              # [新增] 讨论模式内容
│   ├── logs/                   # 系统日志
│   ├── knowledge_base.md       # 知识库
│   ├── tasks.json              # 任务清单
│   └── daily_plan.md           # 每日计划
```

---

## 八、示例工作流

### 场景 1: 正常对话
```
用户："刚才的 Igloo-ai 数据库优化方案不错，记录一下"
↓
[自动触发]
1. capture_user.py 捕获输入
2. Claude 回复技术细节
3. save_conversation.py:
   - 保存原始对话到 raw/2026-04-02_raw.md
   - semantic_analyzer 识别为"知识点"
   - dispatcher 追加到 knowledge_base.md:
     "## Igloo-ai 数据库优化
      - 使用索引优化查询性能
      - 考虑分表策略..."
```

### 场景 2: 讨论模式
```
用户："【讨论】要不要重构整个认证模块？"
↓
[自动触发]
1. save_conversation.py 检测到【讨论】
2. 保存到 proposals/2026-04-02_auth_refactor.md
3. 不写入 knowledge_base.md（等待确认）
4. 提示用户："已保存为提案，确认后运行 promote_proposal.py"
```

### 场景 3: 日终总结
```
用户："总结今天的工作"
↓
[手动或定时触发]
python .claude/scripts/daily_digest.py
↓
输出：
# 2026-04-02 日终总结
## 今日成果
- 完成 Igloo-ai 数据库优化方案设计
- 修复 3 个 bug

## 明日计划
- 实施认证模块重构
- Code review PR #42
```

---

## 九、风险与限制

1. **Hook 事件不稳定**: Claude CLI 的 Hook 系统可能有 bug
   - 缓解：多加日志，监控事件触发
   
2. **API 成本**: 如果接入 Claude API
   - 缓解：先用规则引擎，后期再升级
   
3. **权限问题**: 无法读取 ~/.claude/history.jsonl
   - 缓解：仅依赖项目内 memory/
   
4. **内容分类准确性**: 规则引擎可能误判
   - 缓解：持续优化关键词库，后期接入 AI

---

## 十、成功标准

### 短期（1 周）
- ✅ Hook 系统稳定运行，无丢失对话
- ✅ 知识库不再充斥无用片段
- ✅ 任务清单自动更新，包含明确标题

### 中期（1 月）
- ✅ 讨论模式正常工作，避免误写入
- ✅ 每日总结质量达到可直接使用
- ✅ 历史上下文注入有效，减少重复问题

### 长期（3 月）
- ✅ 知识库成为真正的"第二大脑"
- ✅ 任务清单与外部系统（如 Notion）打通
- ✅ AI 能主动提醒遗留问题和截止任务

---

## 附录：需求文件对照表

| 需求模块 | 设计模块 | 实现文件 | 状态 |
|---------|---------|---------|-----|
| 历史追溯器 | Layer 1: 捕获层 | inject_context.py | ✅ 基础版 |
| 指令中枢 | Layer 1: 捕获层 | CLAUDE.md + settings.json | ⚠️ 需修正 |
| 分发执行器 | Layer 4: 分发层 | save_conversation.py + dispatcher.py | ⚠️ 需重构 |
| 周期性归纳器 | Layer 5: 整理层 | daily_digest.py | ❌ 待开发 |
| 语义化分发 | Layer 3: 分析层 | semantic_analyzer.py | ❌ 待开发 |

---

**总结**: 当前系统已有 60% 基础架构，需要补充语义分析、讨论模式、日终总结三大核心能力。建议采用"分阶段实施、快速迭代验证"策略。
